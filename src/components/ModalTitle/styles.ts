import styled from "styled-components";

import { Row } from "dakota-2-portal/src/styles/global";

export const TitleWrapper = styled(Row).attrs(()=>({
  flex: 1
}))`
  justify-content: flex-start;
`;
